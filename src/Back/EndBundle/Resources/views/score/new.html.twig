{% extends 'base.html.twig' %}

{% block body %}

    <div class="box-header with-border">
        <h3 class="box-title">Score creation</h3>
        <a href="{{ path('score_index') }}" class="btn btn-default" style="float: right;">Back to the list</a>
    </div>

    <div class="container-fluid" style="padding-top: 10px">
        {{ form_start(form) }}
        <div class="form-group row">
            <div class="col-md-2 col-md-offset-2">
                {{ form_label(form.testScore, null, {'label':'Note'}) }}
            </div>
            <div class="col-md-5">
                {{ form_widget(form.testScore, {'attr': {'type':'text','class':'form-control','placeholder':'Enter...' }}) }}
            </div>
        </div>
        <div class="form-group row">
            <div class="col-md-2 col-md-offset-4">
                <input type="submit" value="Create" class="btn btn-primary btn-lg"/>
            </div>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
